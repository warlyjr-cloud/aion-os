[package]
name = "aion_kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
# Dependências agnósticas de plataforma podem ir aqui

[target.'cfg(target_arch = "x86_64")'.dependencies]
bootloader = "0.9"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
